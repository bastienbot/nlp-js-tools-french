'use strict';
var adj = require('./adj');
var adv = require('./adv');
var art = require('./art');
var con = require('./con');
var nom = require('./nom');
var ono = require('./ono');
var pre = require('./pre');
var ver = require('./ver');

module.exports = {
    dicts: {
        adj,
        adv,
        art,
        con,
        nom,
        ono,
        pre,
        ver
    }
}
